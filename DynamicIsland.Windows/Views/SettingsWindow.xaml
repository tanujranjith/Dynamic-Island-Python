<Window x:Class="DynamicIsland.Windows.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dynamic Island Settings" Width="940" Height="700" MinWidth="840" MinHeight="560"
        WindowStartupLocation="CenterScreen" Background="{DynamicResource SettingsBackgroundBrush}"
        Foreground="{DynamicResource SettingsTextBrush}" FontFamily="Segoe UI Variable Text">
    <Window.Resources>
        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI Variable Display" />
            <Setter Property="FontSize" Value="22" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="2,0,0,14" />
        </Style>
        <Style x:Key="GroupHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{DynamicResource SettingsMutedBrush}" />
            <Setter Property="Margin" Value="6,16,0,8" />
        </Style>
        <Style x:Key="RowTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13.5" />
            <Setter Property="Foreground" Value="{DynamicResource SettingsTextBrush}" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style x:Key="RowDesc" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11.5" />
            <Setter Property="Foreground" Value="{DynamicResource SettingsMutedBrush}" />
            <Setter Property="Margin" Value="0,2,12,0" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>
        <Style x:Key="Divider" TargetType="Border">
            <Setter Property="Height" Value="1" />
            <Setter Property="Background" Value="#16FFFFFF" />
            <Setter Property="Margin" Value="0,4" />
        </Style>

        <Style x:Key="ToggleSwitch" TargetType="CheckBox">
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Border x:Name="Track" Width="44" Height="26" CornerRadius="13" Background="#3C4654" BorderBrush="#54606E" BorderThickness="1">
                            <Border x:Name="Thumb" Width="18" Height="18" CornerRadius="9" Background="#CBD4DF" HorizontalAlignment="Left" Margin="4,0,0,0">
                                <Border.RenderTransform><TranslateTransform x:Name="ThumbT" X="0" /></Border.RenderTransform>
                            </Border>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="#2587FF" />
                                <Setter TargetName="Track" Property="BorderBrush" Value="#2587FF" />
                                <Setter TargetName="Thumb" Property="Background" Value="#FFFFFF" />
                                <Trigger.EnterActions><BeginStoryboard><Storyboard>
                                    <DoubleAnimation Storyboard.TargetName="ThumbT" Storyboard.TargetProperty="X" To="18" Duration="0:0:0.18">
                                        <DoubleAnimation.EasingFunction><BackEase EasingMode="EaseOut" Amplitude="0.6" /></DoubleAnimation.EasingFunction>
                                    </DoubleAnimation></Storyboard></BeginStoryboard></Trigger.EnterActions>
                                <Trigger.ExitActions><BeginStoryboard><Storyboard>
                                    <DoubleAnimation Storyboard.TargetName="ThumbT" Storyboard.TargetProperty="X" To="0" Duration="0:0:0.18">
                                        <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut" /></DoubleAnimation.EasingFunction>
                                    </DoubleAnimation></Storyboard></BeginStoryboard></Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ComboItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource SettingsTextBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="b" Background="Transparent" CornerRadius="6" Margin="4,2" Padding="11,8"><ContentPresenter /></Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True"><Setter TargetName="b" Property="Background" Value="#2587FF" /></Trigger>
                            <Trigger Property="IsSelected" Value="True"><Setter TargetName="b" Property="Background" Value="#33558BFF" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SettingsCombo" TargetType="ComboBox">
            <Setter Property="Width" Value="220" /><Setter Property="Height" Value="36" />
            <Setter Property="Cursor" Value="Hand" /><Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="ItemContainerStyle" Value="{StaticResource ComboItemStyle}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Focusable="False" ClickMode="Press" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template><ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="bd" Background="{DynamicResource SettingsInputBrush}" BorderBrush="{DynamicResource SettingsCardBorderBrush}" BorderThickness="1" CornerRadius="9" />
                                    <ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="bd" Property="BorderBrush" Value="#4D8AD8FF" /></Trigger></ControlTemplate.Triggers>
                                </ControlTemplate></ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" Margin="13,0,32,0" VerticalAlignment="Center" IsHitTestVisible="False" TextElement.Foreground="{DynamicResource SettingsTextBrush}" />
                            <TextBlock Text="&#xE70D;" FontFamily="Segoe Fluent Icons" FontSize="10" HorizontalAlignment="Right" Margin="0,0,13,0" VerticalAlignment="Center" Foreground="{DynamicResource SettingsMutedBrush}" IsHitTestVisible="False" />
                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Border Background="{DynamicResource SettingsCardBrush}" BorderBrush="{DynamicResource SettingsCardBorderBrush}" BorderThickness="1" CornerRadius="9" Margin="0,5,0,0" Padding="2" MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <Border.Effect><DropShadowEffect BlurRadius="18" ShadowDepth="3" Opacity="0.45" Color="#000000" /></Border.Effect>
                                    <ScrollViewer MaxHeight="280"><ItemsPresenter /></ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SettingsSlider" TargetType="Slider">
            <Setter Property="Width" Value="178" /><Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Cursor" Value="Hand" /><Setter Property="IsMoveToPointEnabled" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid VerticalAlignment="Center" Height="22">
                            <Border Height="5" CornerRadius="3" Background="#3C4654" VerticalAlignment="Center" />
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton><RepeatButton Command="Slider.DecreaseLarge" Focusable="False">
                                    <RepeatButton.Template><ControlTemplate TargetType="RepeatButton"><Border Height="5" CornerRadius="3" Background="#2587FF" VerticalAlignment="Center" /></ControlTemplate></RepeatButton.Template>
                                </RepeatButton></Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton><RepeatButton Command="Slider.IncreaseLarge" Focusable="False">
                                    <RepeatButton.Template><ControlTemplate TargetType="RepeatButton"><Border Background="Transparent" /></ControlTemplate></RepeatButton.Template>
                                </RepeatButton></Track.IncreaseRepeatButton>
                                <Track.Thumb><Thumb Width="18" Height="18">
                                    <Thumb.Template><ControlTemplate TargetType="Thumb">
                                        <Ellipse Width="18" Height="18" Fill="#FFFFFF"><Ellipse.Effect><DropShadowEffect BlurRadius="6" ShadowDepth="1" Opacity="0.4" /></Ellipse.Effect></Ellipse>
                                    </ControlTemplate></Thumb.Template>
                                </Thumb></Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Text input -->
        <Style x:Key="TextInput" TargetType="TextBox">
            <Setter Property="Height" Value="34" /><Setter Property="Width" Value="160" />
            <Setter Property="VerticalContentAlignment" Value="Center" /><Setter Property="Padding" Value="11,0" />
            <Setter Property="Foreground" Value="{DynamicResource SettingsTextBrush}" />
            <Setter Property="CaretBrush" Value="{DynamicResource SettingsTextBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="bd" Background="{DynamicResource SettingsInputBrush}" BorderBrush="{DynamicResource SettingsCardBorderBrush}" BorderThickness="1" CornerRadius="9">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers><Trigger Property="IsFocused" Value="True"><Setter TargetName="bd" Property="BorderBrush" Value="#2587FF" /></Trigger></ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Sidebar item -->
        <Style x:Key="NavItem" TargetType="ListBoxItem">
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="bd" CornerRadius="9" Margin="8,2" Padding="11,9" Background="Transparent">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Glyph}" FontFamily="Segoe Fluent Icons" FontSize="15" VerticalAlignment="Center" Width="24" Foreground="{DynamicResource SettingsMutedBrush}" />
                                <TextBlock Text="{Binding Name}" FontSize="13.5" VerticalAlignment="Center" Foreground="{DynamicResource SettingsTextBrush}" />
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="bd" Property="Background" Value="#14FFFFFF" /></Trigger>
                            <Trigger Property="IsSelected" Value="True"><Setter TargetName="bd" Property="Background" Value="#2587FF" /></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="244" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- ===== Sidebar ===== -->
        <Border Grid.Column="0" Background="{DynamicResource SettingsHeaderBrush}" BorderBrush="#16FFFFFF" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Margin="18,20,18,12">
                    <Border Width="38" Height="38" CornerRadius="11" Background="#2587FF" VerticalAlignment="Center">
                        <TextBlock Text="&#xE7C4;" FontFamily="Segoe Fluent Icons" FontSize="18" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Dynamic Island" FontFamily="Segoe UI Variable Display" FontSize="15" FontWeight="SemiBold" />
                        <TextBlock Text="Settings" FontSize="11.5" Foreground="{DynamicResource SettingsMutedBrush}" />
                    </StackPanel>
                </StackPanel>
                <Border Grid.Row="1" Margin="14,2,14,8" Height="34" CornerRadius="9" Background="{DynamicResource SettingsInputBrush}" BorderBrush="{DynamicResource SettingsCardBorderBrush}" BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                        <TextBlock Text="&#xE721;" FontFamily="Segoe Fluent Icons" FontSize="13" Margin="11,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource SettingsMutedBrush}" />
                        <TextBox Grid.Column="1" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Margin="8,0,8,0"
                                 Foreground="{DynamicResource SettingsTextBrush}" CaretBrush="{DynamicResource SettingsTextBrush}"
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                    </Grid>
                </Border>
                <ListBox Grid.Row="2" Background="Transparent" BorderThickness="0" ItemsSource="{Binding Sections}"
                         SelectedValuePath="Key" SelectedValue="{Binding SelectedSectionKey}"
                         ItemContainerStyle="{StaticResource NavItem}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" />
                <Button Grid.Row="3" Content="Done" Command="{Binding CloseCommand}" Margin="14" Style="{StaticResource SettingsButton}" />
            </Grid>
        </Border>

        <!-- ===== Content ===== -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Padding="34,26,34,26">
            <Grid>
                <!-- GENERAL -->
                <StackPanel Visibility="{Binding SelectedSectionKey, Converter={StaticResource SectionVisibility}, ConverterParameter=general}">
                    <TextBlock Text="General" Style="{StaticResource SectionTitle}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <StackPanel><TextBlock Text="Launch on startup" Style="{StaticResource RowTitle}" /><TextBlock Text="Open the island automatically when you sign in." Style="{StaticResource RowDesc}" /></StackPanel>
                            <CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding LaunchOnStartup}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <TextBlock Text="Always on top" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding AlwaysOnTop}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <StackPanel><TextBlock Text="Always expanded" Style="{StaticResource RowTitle}" /><TextBlock Text="Keep the island open instead of collapsing." Style="{StaticResource RowDesc}" /></StackPanel>
                            <CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding AlwaysExpanded}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <TextBlock Text="Expand on hover" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ExpandOnHover}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <StackPanel><TextBlock Text="Lock position" Style="{StaticResource RowTitle}" /><TextBlock Text="Prevent dragging the island out of place." Style="{StaticResource RowDesc}" /></StackPanel>
                            <CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding LockPosition}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <StackPanel><TextBlock Text="Click-through when compact" Style="{StaticResource RowTitle}" /><TextBlock Text="Pass clicks to apps behind it." Style="{StaticResource RowDesc}" /></StackPanel>
                            <CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ClickThroughWhenCompact}" /></Grid>
                    </StackPanel></Border>
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <StackPanel><TextBlock Text="Auto-hide in fullscreen" Style="{StaticResource RowTitle}" /><TextBlock Text="Hide while a fullscreen app or game is focused." Style="{StaticResource RowDesc}" /></StackPanel>
                            <CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding AutoHideFullscreen}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <StackPanel><TextBlock Text="Idle dimming" Style="{StaticResource RowTitle}" /><TextBlock Text="Fade the island when left alone; restore on hover." Style="{StaticResource RowDesc}" /></StackPanel>
                            <CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding IdleDimming}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <TextBlock Text="Idle opacity" Style="{StaticResource RowTitle}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal"><Slider Style="{StaticResource SettingsSlider}" Minimum="20" Maximum="100" Value="{Binding IdleOpacityPercent}" TickFrequency="5" IsSnapToTickEnabled="True" /><TextBlock Text="{Binding IdleOpacityPercent, StringFormat={}{0}%}" Width="44" TextAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource SettingsMutedBrush}" /></StackPanel></Grid>
                    </StackPanel></Border>
                </StackPanel>

                <!-- APPEARANCE -->
                <StackPanel Visibility="{Binding SelectedSectionKey, Converter={StaticResource SectionVisibility}, ConverterParameter=appearance}">
                    <TextBlock Text="Appearance" Style="{StaticResource SectionTitle}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <TextBlock Text="Theme" Style="{StaticResource RowTitle}" /><ComboBox Grid.Column="1" Style="{StaticResource SettingsCombo}" ItemsSource="{Binding ThemeOptions}" SelectedItem="{Binding Theme}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <TextBlock Text="Font" Style="{StaticResource RowTitle}" /><ComboBox Grid.Column="1" Style="{StaticResource SettingsCombo}" ItemsSource="{Binding FontOptions}" SelectedItem="{Binding FontFamilyName}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <StackPanel><TextBlock Text="Liquid glass" Style="{StaticResource RowTitle}" /><TextBlock Text="Translucent frosted glass material." Style="{StaticResource RowDesc}" /></StackPanel>
                            <CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding LiquidGlass}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <TextBlock Text="Glass transparency" Style="{StaticResource RowTitle}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal"><Slider Style="{StaticResource SettingsSlider}" Minimum="0" Maximum="80" Value="{Binding GlassTransparency}" TickFrequency="5" IsSnapToTickEnabled="True" /><TextBlock Text="{Binding GlassTransparency, StringFormat={}{0}%}" Width="44" TextAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource SettingsMutedBrush}" /></StackPanel></Grid>
                    </StackPanel></Border>
                    <TextBlock Text="COLOURS" Style="{StaticResource GroupHeader}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <StackPanel><TextBlock Text="Custom colours" Style="{StaticResource RowTitle}" /><TextBlock Text="Override accent, text and glass colours." Style="{StaticResource RowDesc}" /></StackPanel>
                            <CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding UseCustomColors}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <StackPanel><TextBlock Text="Adaptive accent" Style="{StaticResource RowTitle}" /><TextBlock Text="Pull the accent colour from the album art." Style="{StaticResource RowDesc}" /></StackPanel>
                            <CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding AdaptiveAccent}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <TextBlock Text="Accent colour" Style="{StaticResource RowTitle}" Margin="0,2,0,8" />
                        <ItemsControl ItemsSource="{Binding AccentSwatches}">
                            <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate><DataTemplate>
                                <Button Width="30" Height="30" Margin="0,0,8,8" Cursor="Hand"
                                        Command="{Binding DataContext.PickAccentCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}">
                                    <Button.Template><ControlTemplate TargetType="Button">
                                        <Border CornerRadius="15" Background="{Binding}" BorderBrush="#55FFFFFF" BorderThickness="2" />
                                    </ControlTemplate></Button.Template>
                                </Button>
                            </DataTemplate></ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Grid Margin="0,4,0,0"><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <TextBlock Text="Accent hex" Style="{StaticResource RowTitle}" /><TextBox Grid.Column="1" Style="{StaticResource TextInput}" Text="{Binding AccentColorHex, UpdateSourceTrigger=LostFocus}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <StackPanel><TextBlock Text="Text colour hex" Style="{StaticResource RowTitle}" /><TextBlock Text="Leave blank to follow the theme." Style="{StaticResource RowDesc}" /></StackPanel>
                            <TextBox Grid.Column="1" Style="{StaticResource TextInput}" Text="{Binding TextColorHex, UpdateSourceTrigger=LostFocus}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <StackPanel><TextBlock Text="Glass colour hex" Style="{StaticResource RowTitle}" /><TextBlock Text="Leave blank for the default tint." Style="{StaticResource RowDesc}" /></StackPanel>
                            <TextBox Grid.Column="1" Style="{StaticResource TextInput}" Text="{Binding GlassColorHex, UpdateSourceTrigger=LostFocus}" /></Grid>
                    </StackPanel></Border>
                    <TextBlock Text="SKINS" Style="{StaticResource GroupHeader}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <TextBlock Text="One-tap looks — sets colours, glass and font together." Style="{StaticResource RowDesc}" Margin="0,0,0,8" />
                        <ItemsControl ItemsSource="{Binding ThemeSkins}">
                            <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate></ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate><DataTemplate>
                                <Button Content="{Binding}" Command="{Binding DataContext.ApplySkinCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}"
                                        Style="{StaticResource SettingsButton}" Background="{DynamicResource SettingsSubtleButtonBrush}" Foreground="{DynamicResource SettingsTextBrush}" Margin="0,0,8,8" />
                            </DataTemplate></ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel></Border>
                </StackPanel>

                <!-- SIZES -->
                <StackPanel Visibility="{Binding SelectedSectionKey, Converter={StaticResource SectionVisibility}, ConverterParameter=sizes}">
                    <TextBlock Text="Text &amp; Sizes" Style="{StaticResource SectionTitle}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <StackPanel><TextBlock Text="Interface scale" Style="{StaticResource RowTitle}" /><TextBlock Text="Master multiplier for every element." Style="{StaticResource RowDesc}" /></StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal"><Slider Style="{StaticResource SettingsSlider}" Minimum="70" Maximum="150" Value="{Binding InterfaceScale}" TickFrequency="5" IsSnapToTickEnabled="True" /><TextBlock Text="{Binding InterfaceScale, StringFormat={}{0}%}" Width="44" TextAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource SettingsMutedBrush}" /></StackPanel></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Clock" Style="{StaticResource RowTitle}" /><StackPanel Grid.Column="1" Orientation="Horizontal"><Slider Style="{StaticResource SettingsSlider}" Minimum="60" Maximum="160" Value="{Binding ClockSize}" TickFrequency="5" IsSnapToTickEnabled="True" /><TextBlock Text="{Binding ClockSize, StringFormat={}{0}%}" Width="44" TextAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource SettingsMutedBrush}" /></StackPanel></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Date" Style="{StaticResource RowTitle}" /><StackPanel Grid.Column="1" Orientation="Horizontal"><Slider Style="{StaticResource SettingsSlider}" Minimum="60" Maximum="160" Value="{Binding DateSize}" TickFrequency="5" IsSnapToTickEnabled="True" /><TextBlock Text="{Binding DateSize, StringFormat={}{0}%}" Width="44" TextAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource SettingsMutedBrush}" /></StackPanel></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Battery &amp; charging" Style="{StaticResource RowTitle}" /><StackPanel Grid.Column="1" Orientation="Horizontal"><Slider Style="{StaticResource SettingsSlider}" Minimum="60" Maximum="160" Value="{Binding BatterySize}" TickFrequency="5" IsSnapToTickEnabled="True" /><TextBlock Text="{Binding BatterySize, StringFormat={}{0}%}" Width="44" TextAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource SettingsMutedBrush}" /></StackPanel></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Media title" Style="{StaticResource RowTitle}" /><StackPanel Grid.Column="1" Orientation="Horizontal"><Slider Style="{StaticResource SettingsSlider}" Minimum="60" Maximum="160" Value="{Binding MediaTitleSize}" TickFrequency="5" IsSnapToTickEnabled="True" /><TextBlock Text="{Binding MediaTitleSize, StringFormat={}{0}%}" Width="44" TextAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource SettingsMutedBrush}" /></StackPanel></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Media artist" Style="{StaticResource RowTitle}" /><StackPanel Grid.Column="1" Orientation="Horizontal"><Slider Style="{StaticResource SettingsSlider}" Minimum="60" Maximum="160" Value="{Binding MediaArtistSize}" TickFrequency="5" IsSnapToTickEnabled="True" /><TextBlock Text="{Binding MediaArtistSize, StringFormat={}{0}%}" Width="44" TextAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource SettingsMutedBrush}" /></StackPanel></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Volume" Style="{StaticResource RowTitle}" /><StackPanel Grid.Column="1" Orientation="Horizontal"><Slider Style="{StaticResource SettingsSlider}" Minimum="60" Maximum="160" Value="{Binding VolumeSize}" TickFrequency="5" IsSnapToTickEnabled="True" /><TextBlock Text="{Binding VolumeSize, StringFormat={}{0}%}" Width="44" TextAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource SettingsMutedBrush}" /></StackPanel></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Camera status" Style="{StaticResource RowTitle}" /><StackPanel Grid.Column="1" Orientation="Horizontal"><Slider Style="{StaticResource SettingsSlider}" Minimum="60" Maximum="160" Value="{Binding VisionTextSize}" TickFrequency="5" IsSnapToTickEnabled="True" /><TextBlock Text="{Binding VisionTextSize, StringFormat={}{0}%}" Width="44" TextAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource SettingsMutedBrush}" /></StackPanel></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Compact text" Style="{StaticResource RowTitle}" /><StackPanel Grid.Column="1" Orientation="Horizontal"><Slider Style="{StaticResource SettingsSlider}" Minimum="60" Maximum="160" Value="{Binding CompactTextSize}" TickFrequency="5" IsSnapToTickEnabled="True" /><TextBlock Text="{Binding CompactTextSize, StringFormat={}{0}%}" Width="44" TextAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource SettingsMutedBrush}" /></StackPanel></Grid>
                    </StackPanel></Border>
                    <TextBlock Text="ALBUM ART" Style="{StaticResource GroupHeader}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Album art size" Style="{StaticResource RowTitle}" /><StackPanel Grid.Column="1" Orientation="Horizontal"><Slider Style="{StaticResource SettingsSlider}" Minimum="70" Maximum="130" Value="{Binding AlbumArtSize}" TickFrequency="5" IsSnapToTickEnabled="True" /><TextBlock Text="{Binding AlbumArtSize, StringFormat={}{0}%}" Width="44" TextAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource SettingsMutedBrush}" /></StackPanel></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Expanded view album art size" Style="{StaticResource RowTitle}" /><TextBlock Text="Size of the album cover in the expanded island." Style="{StaticResource RowDesc}" /></StackPanel><StackPanel Grid.Column="1" Orientation="Horizontal"><Slider Style="{StaticResource SettingsSlider}" Minimum="40" Maximum="160" Value="{Binding ExpandedAlbumArtSize}" TickFrequency="5" IsSnapToTickEnabled="True" /><TextBlock Text="{Binding ExpandedAlbumArtSize, StringFormat={}{0}%}" Width="44" TextAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource SettingsMutedBrush}" /></StackPanel></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Album corner radius" Style="{StaticResource RowTitle}" /><TextBlock Text="0 = square, ~30 = squircle, 50 = circle." Style="{StaticResource RowDesc}" /></StackPanel><StackPanel Grid.Column="1" Orientation="Horizontal"><Slider Style="{StaticResource SettingsSlider}" Minimum="0" Maximum="50" Value="{Binding AlbumCornerRadius}" TickFrequency="2" IsSnapToTickEnabled="True" /><TextBlock Text="{Binding AlbumCornerRadius, StringFormat={}{0}%}" Width="44" TextAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource SettingsMutedBrush}" /></StackPanel></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Playback progress ring" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowMediaProgressRing}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Song time remaining" Style="{StaticResource RowTitle}" /><TextBlock Text="Count the trailing time down instead of showing the total length." Style="{StaticResource RowDesc}" /></StackPanel><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowSongTimeRemaining}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Scroll long titles" Style="{StaticResource RowTitle}" /><TextBlock Text="Cycle titles that don't fit so the full name is readable." Style="{StaticResource RowDesc}" /></StackPanel><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ScrollLongTitles}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Timer ring" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowTimerRing}" /></Grid>
                    </StackPanel></Border>
                </StackPanel>

                <!-- LAYOUT -->
                <StackPanel Visibility="{Binding SelectedSectionKey, Converter={StaticResource SectionVisibility}, ConverterParameter=layout}">
                    <TextBlock Text="Layout &amp; Position" Style="{StaticResource SectionTitle}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Island size" Style="{StaticResource RowTitle}" /><ComboBox Grid.Column="1" Style="{StaticResource SettingsCombo}" ItemsSource="{Binding SizeOptions}" SelectedItem="{Binding IslandSize}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Island corner radius" Style="{StaticResource RowTitle}" /><TextBlock Text="Lower = sharper corners, higher = rounder." Style="{StaticResource RowDesc}" /></StackPanel><StackPanel Grid.Column="1" Orientation="Horizontal"><Slider Style="{StaticResource SettingsSlider}" Minimum="0" Maximum="48" Value="{Binding IslandCornerRadius}" TickFrequency="2" IsSnapToTickEnabled="True" /><TextBlock Text="{Binding IslandCornerRadius, StringFormat={}{0}px}" Width="44" TextAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource SettingsMutedBrush}" /></StackPanel></Grid>
                        <!-- Live shape preview: scaled-down expanded + mini island silhouettes -->
                        <Border Margin="0,10,0,0" Padding="18,16" CornerRadius="10" Background="#0E0F14" BorderBrush="#232B36" BorderThickness="1">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <!-- Expanded preview -->
                                <StackPanel Margin="0,0,22,0" HorizontalAlignment="Center">
                                    <Border Width="212" Height="48" CornerRadius="{Binding PreviewExpandedCorner}" Background="#2A2F37" BorderBrush="#3D4A5C" BorderThickness="1" Padding="8,0">
                                        <Grid VerticalAlignment="Center">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                            <Border Width="30" Height="30" CornerRadius="7" Background="#3A6AB0" />
                                            <StackPanel Grid.Column="1" Margin="9,0,0,0" VerticalAlignment="Center">
                                                <Rectangle Height="5" Width="92" RadiusX="2.5" RadiusY="2.5" Fill="#9FB0C6" HorizontalAlignment="Left" />
                                                <Rectangle Height="4" Width="60" RadiusX="2" RadiusY="2" Fill="#5C6B82" HorizontalAlignment="Left" Margin="0,5,0,0" />
                                            </StackPanel>
                                            <Ellipse Grid.Column="2" Width="9" Height="9" Fill="#5C6B82" VerticalAlignment="Center" />
                                        </Grid>
                                    </Border>
                                    <TextBlock Text="Expanded" FontSize="10" HorizontalAlignment="Center" Margin="0,7,0,0" Foreground="{DynamicResource SettingsMutedBrush}" />
                                </StackPanel>
                                <!-- Mini preview -->
                                <StackPanel HorizontalAlignment="Center">
                                    <Border Width="104" Height="24" CornerRadius="{Binding PreviewMiniCorner}" Background="#2A2F37" BorderBrush="#3D4A5C" BorderThickness="1" Padding="7,0" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Ellipse Width="11" Height="11" Fill="#3A6AB0" VerticalAlignment="Center" />
                                            <Rectangle Height="4" Width="48" RadiusX="2" RadiusY="2" Fill="#9FB0C6" VerticalAlignment="Center" Margin="7,0,0,0" />
                                        </StackPanel>
                                    </Border>
                                    <TextBlock Text="Mini" FontSize="10" HorizontalAlignment="Center" Margin="0,7,0,0" Foreground="{DynamicResource SettingsMutedBrush}" />
                                </StackPanel>
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Animation" Style="{StaticResource RowTitle}" /><ComboBox Grid.Column="1" Style="{StaticResource SettingsCombo}" ItemsSource="{Binding AnimationOptions}" SelectedItem="{Binding AnimationIntensity}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Auto-grow pill" Style="{StaticResource RowTitle}" /><TextBlock Text="Resize the island to fit large text (no clipping)." Style="{StaticResource RowDesc}" /></StackPanel><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding AutoGrowPill}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Default position" Style="{StaticResource RowTitle}" /><ComboBox Grid.Column="1" Style="{StaticResource SettingsCombo}" ItemsSource="{Binding PositionOptions}" SelectedItem="{Binding DefaultPosition}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Monitor" Style="{StaticResource RowTitle}" /><ComboBox Grid.Column="1" Style="{StaticResource SettingsCombo}" ItemsSource="{Binding MonitorOptions}" SelectedItem="{Binding PreferredMonitor}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Follow active screen" Style="{StaticResource RowTitle}" /><TextBlock Text="Jump to the monitor with the focused window." Style="{StaticResource RowDesc}" /></StackPanel><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding FollowActiveScreen}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Distance from top" Style="{StaticResource RowTitle}" /><StackPanel Grid.Column="1" Orientation="Horizontal"><Slider Style="{StaticResource SettingsSlider}" Minimum="0" Maximum="120" Value="{Binding TopOffset}" TickFrequency="2" IsSnapToTickEnabled="True" /><TextBlock Text="{Binding TopOffset, StringFormat={}{0} px}" Width="44" TextAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource SettingsMutedBrush}" /></StackPanel></Grid>
                    </StackPanel></Border>
                    <TextBlock Text="EXPANDED ORDER" Style="{StaticResource GroupHeader}" />
                    <Border Style="{StaticResource SettingsCard}">
                        <ItemsControl ItemsSource="{Binding ModuleOrder}">
                            <ItemsControl.ItemTemplate><DataTemplate>
                                <Grid Margin="0,4"><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Name}" Style="{StaticResource RowTitle}" />
                                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                                        <Button Content="&#xE74A;" FontFamily="Segoe Fluent Icons" Width="34" Height="30" Margin="0,0,6,0" Style="{StaticResource SettingsButton}" Background="{DynamicResource SettingsSubtleButtonBrush}" Foreground="{DynamicResource SettingsTextBrush}" Command="{Binding DataContext.MoveModuleUpCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" />
                                        <Button Content="&#xE74B;" FontFamily="Segoe Fluent Icons" Width="34" Height="30" Style="{StaticResource SettingsButton}" Background="{DynamicResource SettingsSubtleButtonBrush}" Foreground="{DynamicResource SettingsTextBrush}" Command="{Binding DataContext.MoveModuleDownCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" />
                                    </StackPanel>
                                </Grid>
                            </DataTemplate></ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Border>
                </StackPanel>

                <!-- MEDIA -->
                <StackPanel Visibility="{Binding SelectedSectionKey, Converter={StaticResource SectionVisibility}, ConverterParameter=media}">
                    <TextBlock Text="Media" Style="{StaticResource SectionTitle}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Now playing media" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowMedia}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Album art in compact" Style="{StaticResource RowTitle}" /><TextBlock Text="Show the cover instead of the icon when playing." Style="{StaticResource RowDesc}" /></StackPanel><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowAlbumArtInCompact}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Click art opens the app" Style="{StaticResource RowTitle}" /><TextBlock Text="Click the album art to jump to the player." Style="{StaticResource RowDesc}" /></StackPanel><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ClickArtOpensApp}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Preferred media app" Style="{StaticResource RowTitle}" /><ComboBox Grid.Column="1" Style="{StaticResource SettingsCombo}" ItemsSource="{Binding AvailableMediaApps}" SelectedItem="{Binding SelectedMediaApp}" /></Grid>
                    </StackPanel></Border>
                </StackPanel>

                <!-- LIVE ACTIVITIES -->
                <StackPanel Visibility="{Binding SelectedSectionKey, Converter={StaticResource SectionVisibility}, ConverterParameter=activities}">
                    <TextBlock Text="Live Activities" Style="{StaticResource SectionTitle}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Weather" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowWeather}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Location" Style="{StaticResource RowTitle}" /><TextBlock Text="City name, e.g. London." Style="{StaticResource RowDesc}" /></StackPanel><TextBox Grid.Column="1" Style="{StaticResource TextInput}" Text="{Binding WeatherLocation, UpdateSourceTrigger=LostFocus}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Use Fahrenheit" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding WeatherFahrenheit}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="System monitor" Style="{StaticResource RowTitle}" /><TextBlock Text="Show RAM and network in the island. (CPU is omitted on virtualized PCs where the OS counters don't match Task Manager.)" Style="{StaticResource RowDesc}" /></StackPanel><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowSystemMonitor}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="RAM in compact view" Style="{StaticResource RowTitle}" /><TextBlock Text="Show memory usage on the collapsed island." Style="{StaticResource RowDesc}" /></StackPanel><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowRamInCompact}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Real audio spectrum" Style="{StaticResource RowTitle}" /><TextBlock Text="Live frequency bars from system audio." Style="{StaticResource RowDesc}" /></StackPanel><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding RealAudioSpectrum}" /></Grid>
                    </StackPanel></Border>
                    <TextBlock Text="VOLUME WARNING" Style="{StaticResource GroupHeader}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="High-volume warning" Style="{StaticResource RowTitle}" /><TextBlock Text="Show a notice in the island when volume exceeds the threshold." Style="{StaticResource RowDesc}" /></StackPanel><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding VolumeWarningEnabled}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Warn at" Style="{StaticResource RowTitle}" /><StackPanel Grid.Column="1" Orientation="Horizontal"><Slider Style="{StaticResource SettingsSlider}" Minimum="10" Maximum="100" Value="{Binding VolumeWarningThreshold}" TickFrequency="5" IsSnapToTickEnabled="True" /><TextBlock Text="{Binding VolumeWarningThreshold, StringFormat={}{0}%}" Width="44" TextAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource SettingsMutedBrush}" /></StackPanel></Grid>
                    </StackPanel></Border>
                    <TextBlock Text="BATTERY" Style="{StaticResource GroupHeader}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Time remaining" Style="{StaticResource RowTitle}" /><TextBlock Text="Show estimated battery time left on the island." Style="{StaticResource RowDesc}" /></StackPanel><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowBatteryTime}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Low-battery warning" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding LowBatteryWarning}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Warn at" Style="{StaticResource RowTitle}" /><StackPanel Grid.Column="1" Orientation="Horizontal"><Slider Style="{StaticResource SettingsSlider}" Minimum="5" Maximum="50" Value="{Binding LowBatteryThreshold}" TickFrequency="5" IsSnapToTickEnabled="True" /><TextBlock Text="{Binding LowBatteryThreshold, StringFormat={}{0}%}" Width="44" TextAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource SettingsMutedBrush}" /></StackPanel></Grid>
                    </StackPanel></Border>
                    <TextBlock Text="SHOW ON THE ISLAND" Style="{StaticResource GroupHeader}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Clock" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowClock}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Date" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowDate}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="24-hour clock" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding Use24HourClock}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Show seconds" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowSeconds}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Volume &amp; audio" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowVolume}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Battery" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowBattery}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Timer &amp; alarm" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowTimerAlarm}" /></Grid>
                    </StackPanel></Border>
                </StackPanel>

                <!-- WIDGETS -->
                <StackPanel Visibility="{Binding SelectedSectionKey, Converter={StaticResource SectionVisibility}, ConverterParameter=widgets}">
                    <TextBlock Text="Widgets" Style="{StaticResource SectionTitle}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Countdown" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowCountdown}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Label" Style="{StaticResource RowTitle}" /><TextBox Grid.Column="1" Style="{StaticResource TextInput}" Text="{Binding CountdownLabel, UpdateSourceTrigger=LostFocus}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Target date" Style="{StaticResource RowTitle}" /><TextBlock Text="Format: YYYY-MM-DD" Style="{StaticResource RowDesc}" /></StackPanel><TextBox Grid.Column="1" Style="{StaticResource TextInput}" Text="{Binding CountdownDate, UpdateSourceTrigger=LostFocus}" /></Grid>
                    </StackPanel></Border>
                    <TextBlock Text="MARKETS" Style="{StaticResource GroupHeader}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Stocks / crypto ticker" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowStocks}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Symbols" Style="{StaticResource RowTitle}" /><TextBlock Text="Comma-separated, e.g. AAPL,MSFT,BTC-USD" Style="{StaticResource RowDesc}" /></StackPanel><TextBox Grid.Column="1" Style="{StaticResource TextInput}" Text="{Binding StockSymbols, UpdateSourceTrigger=LostFocus}" /></Grid>
                    </StackPanel></Border>
                    <TextBlock Text="WORLD CLOCKS" Style="{StaticResource GroupHeader}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="World clocks" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowWorldClocks}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Time zones" Style="{StaticResource RowTitle}" /><TextBlock Text="Comma-separated Windows zone IDs, e.g. Tokyo Standard Time,GMT Standard Time" Style="{StaticResource RowDesc}" /></StackPanel><TextBox Grid.Column="1" Style="{StaticResource TextInput}" Text="{Binding WorldClockZones, UpdateSourceTrigger=LostFocus}" /></Grid>
                    </StackPanel></Border>
                    <TextBlock Text="QUICK LAUNCH" Style="{StaticResource GroupHeader}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Quick-launch buttons" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowQuickLaunch}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <TextBlock Text="Buttons that appear on the expanded island. Add an app, rename it, or reorder." Style="{StaticResource RowDesc}" Margin="0,2,0,6" />
                        <ItemsControl ItemsSource="{Binding QuickLaunchList}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="130" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBox Style="{StaticResource TextInput}" Text="{Binding Name, UpdateSourceTrigger=LostFocus}" ToolTip="Label" />
                                        <TextBox Grid.Column="1" Style="{StaticResource TextInput}" Margin="8,0" Text="{Binding Path, UpdateSourceTrigger=LostFocus}" ToolTip="Path or URL" />
                                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                                            <Button Content="&#xE838;" FontFamily="Segoe Fluent Icons" ToolTip="Browse…" Style="{StaticResource SettingsButton}" Background="{DynamicResource SettingsSubtleButtonBrush}" Foreground="{DynamicResource SettingsTextBrush}" Padding="9,6" Margin="0,0,4,0"
                                                    Command="{Binding DataContext.BrowseQuickLaunchCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" />
                                            <Button Content="&#xE70E;" FontFamily="Segoe Fluent Icons" ToolTip="Move up" Style="{StaticResource SettingsButton}" Background="{DynamicResource SettingsSubtleButtonBrush}" Foreground="{DynamicResource SettingsTextBrush}" Padding="9,6" Margin="0,0,4,0"
                                                    Command="{Binding DataContext.MoveQuickLaunchUpCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" />
                                            <Button Content="&#xE70D;" FontFamily="Segoe Fluent Icons" ToolTip="Move down" Style="{StaticResource SettingsButton}" Background="{DynamicResource SettingsSubtleButtonBrush}" Foreground="{DynamicResource SettingsTextBrush}" Padding="9,6" Margin="0,0,4,0"
                                                    Command="{Binding DataContext.MoveQuickLaunchDownCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" />
                                            <Button Content="&#xE74D;" FontFamily="Segoe Fluent Icons" ToolTip="Remove" Style="{StaticResource SettingsButton}" Background="#3A2630" Foreground="#FFB4B4" Padding="9,6"
                                                    Command="{Binding DataContext.RemoveQuickLaunchCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" />
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Button Content="&#xE710;  Add app…" FontFamily="Segoe Fluent Icons" HorizontalAlignment="Left" Margin="0,8,0,0"
                                Style="{StaticResource SettingsButton}" Command="{Binding AddQuickLaunchCommand}" />
                    </StackPanel></Border>
                    <TextBlock Text="WINDOWS INTEGRATIONS (may require permission)" Style="{StaticResource GroupHeader}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Notification mirroring" Style="{StaticResource RowTitle}" /><TextBlock Text="Show incoming Windows notifications in the island." Style="{StaticResource RowDesc}" /></StackPanel><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowNotifications}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Filter apps" Style="{StaticResource RowTitle}" /><TextBlock Text="All apps, only listed apps (allowlist), or hide listed apps (blocklist)." Style="{StaticResource RowDesc}" /></StackPanel><ComboBox Grid.Column="1" Style="{StaticResource SettingsCombo}" MinWidth="130" ItemsSource="{Binding NotificationFilterOptions}" SelectedItem="{Binding NotificationFilterMode}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="App list" Style="{StaticResource RowTitle}" /><TextBlock Text="Comma-separated app names, e.g. Slack,Mail,Teams" Style="{StaticResource RowDesc}" /></StackPanel><TextBox Grid.Column="1" Style="{StaticResource TextInput}" Text="{Binding NotificationAppFilter, UpdateSourceTrigger=LostFocus}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Mic &amp; camera in-use dots" Style="{StaticResource RowTitle}" /><TextBlock Text="Show an indicator when an app is using your microphone or webcam." Style="{StaticResource RowDesc}" /></StackPanel><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowPrivacyIndicators}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Next meeting + Join" Style="{StaticResource RowTitle}" /><TextBlock Text="Your next calendar event with a one-click join link." Style="{StaticResource RowDesc}" /></StackPanel><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowNextMeeting}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Clipboard history button" Style="{StaticResource RowTitle}" /><TextBlock Text="Quick access to recent copies (needs Win+V history)." Style="{StaticResource RowDesc}" /></StackPanel><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowClipboard}" /></Grid>
                    </StackPanel></Border>
                </StackPanel>

                <!-- CAMERA -->
                <StackPanel Visibility="{Binding SelectedSectionKey, Converter={StaticResource SectionVisibility}, ConverterParameter=camera}">
                    <TextBlock Text="Camera" Style="{StaticResource SectionTitle}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Camera presence &amp; face login" Style="{StaticResource RowTitle}" /><TextBlock Text="Live preview, enrolment and privacy mode on its own page." Style="{StaticResource RowDesc}" /></StackPanel><Button Grid.Column="1" Content="Open camera page" Command="{Binding OpenVisionPageCommand}" Style="{StaticResource SettingsButton}" Background="{DynamicResource SettingsSubtleButtonBrush}" Foreground="{DynamicResource SettingsTextBrush}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Show status on the island" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowVisionStatus}" /></Grid>
                    </StackPanel></Border>
                    <TextBlock Text="AUTOMATIONS" Style="{StaticResource GroupHeader}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Lock on unknown person" Style="{StaticResource RowTitle}" /><TextBlock Text="Lock Windows when a stranger is detected (privacy mode)." Style="{StaticResource RowDesc}" /></StackPanel><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding AutoLockOnUnknown}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Lock delay" Style="{StaticResource RowTitle}" /><StackPanel Grid.Column="1" Orientation="Horizontal"><Slider Style="{StaticResource SettingsSlider}" Minimum="2" Maximum="60" Value="{Binding AutoLockDelaySeconds}" TickFrequency="1" IsSnapToTickEnabled="True" /><TextBlock Text="{Binding AutoLockDelaySeconds, StringFormat={}{0}s}" Width="44" TextAlignment="Right" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource SettingsMutedBrush}" /></StackPanel></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Pause media when away" Style="{StaticResource RowTitle}" /><TextBlock Text="Pause playback when you leave, resume when you return." Style="{StaticResource RowDesc}" /></StackPanel><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding PresenceAwareMedia}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Privacy blur" Style="{StaticResource RowTitle}" /><TextBlock Text="Frost the screen when an unknown person appears." Style="{StaticResource RowDesc}" /></StackPanel><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding PrivacyAutoBlur}" /></Grid>
                    </StackPanel></Border>
                </StackPanel>

                <!-- ADVANCED -->
                <StackPanel Visibility="{Binding SelectedSectionKey, Converter={StaticResource SectionVisibility}, ConverterParameter=advanced}">
                    <TextBlock Text="Advanced" Style="{StaticResource SectionTitle}" />
                    <TextBlock Text="PROFILES" Style="{StaticResource GroupHeader}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <ComboBox Style="{StaticResource SettingsCombo}" Width="220" HorizontalAlignment="Left" ItemsSource="{Binding Presets}" SelectedItem="{Binding SelectedPreset}" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button Content="Apply" Command="{Binding ApplyPresetCommand}" Style="{StaticResource SettingsButton}" Background="{DynamicResource SettingsSubtleButtonBrush}" Foreground="{DynamicResource SettingsTextBrush}" Margin="0,0,8,0" />
                                <Button Content="Delete" Command="{Binding DeletePresetCommand}" Style="{StaticResource SettingsButton}" Background="#3A2630" Foreground="#FFB4B4" />
                            </StackPanel></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <TextBox Style="{StaticResource TextInput}" Width="220" HorizontalAlignment="Left" Text="{Binding NewPresetName, UpdateSourceTrigger=PropertyChanged}" />
                            <Button Grid.Column="1" Content="Save current as preset" Command="{Binding SavePresetCommand}" Style="{StaticResource SettingsButton}" /></Grid>
                    </StackPanel></Border>
                    <TextBlock Text="BACKUP" Style="{StaticResource GroupHeader}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Import / export settings" Style="{StaticResource RowTitle}" /><StackPanel Grid.Column="1" Orientation="Horizontal"><Button Content="Import" Command="{Binding ImportCommand}" Style="{StaticResource SettingsButton}" Background="{DynamicResource SettingsSubtleButtonBrush}" Foreground="{DynamicResource SettingsTextBrush}" Margin="0,0,8,0" /><Button Content="Export" Command="{Binding ExportCommand}" Style="{StaticResource SettingsButton}" Background="{DynamicResource SettingsSubtleButtonBrush}" Foreground="{DynamicResource SettingsTextBrush}" /></StackPanel></Grid>
                    </StackPanel></Border>
                    <TextBlock Text="DEVELOPER" Style="{StaticResource GroupHeader}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Debug overlay" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding DebugOverlay}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Debug logging" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding DebugLogging}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Show in Alt-Tab" Style="{StaticResource RowTitle}" /><CheckBox Grid.Column="1" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding ShowInAltTab}" /></Grid>
                    </StackPanel></Border>
                </StackPanel>

                <!-- ABOUT -->
                <StackPanel Visibility="{Binding SelectedSectionKey, Converter={StaticResource SectionVisibility}, ConverterParameter=about}">
                    <TextBlock Text="About" Style="{StaticResource SectionTitle}" />
                    <Border Style="{StaticResource SettingsCard}"><StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,8">
                            <Border Width="52" Height="52" CornerRadius="15" Background="#2587FF" VerticalAlignment="Center"><TextBlock Text="&#xE7C4;" FontFamily="Segoe Fluent Icons" FontSize="24" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" /></Border>
                            <StackPanel Margin="16,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="Dynamic Island for Windows" FontFamily="Segoe UI Variable Display" FontSize="18" FontWeight="SemiBold" />
                                <TextBlock Text="A native overlay with media, camera, weather and more." FontSize="12" Foreground="{DynamicResource SettingsMutedBrush}" Margin="0,2,0,0" />
                            </StackPanel>
                        </StackPanel>
                        <Border Style="{StaticResource Divider}" />
                        <Grid Margin="0,4,0,0"><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <StackPanel><TextBlock Text="Recenter island" Style="{StaticResource RowTitle}" /><TextBlock Text="Move the island back to the top centre." Style="{StaticResource RowDesc}" /></StackPanel>
                            <Button Grid.Column="1" Content="Recenter" Command="{Binding RecenterCommand}" Style="{StaticResource SettingsButton}" Background="{DynamicResource SettingsSubtleButtonBrush}" Foreground="{DynamicResource SettingsTextBrush}" /></Grid>
                        <Border Style="{StaticResource Divider}" />
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <StackPanel><TextBlock Text="Reset everything" Style="{StaticResource RowTitle}" /><TextBlock Text="Restore all settings to their defaults." Style="{StaticResource RowDesc}" /></StackPanel>
                            <Button Grid.Column="1" Content="Reset to defaults" Command="{Binding ResetCommand}" Style="{StaticResource SettingsButton}" Background="#3A2630" Foreground="#FFB4B4" /></Grid>
                    </StackPanel></Border>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
